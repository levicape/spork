########################################
# THIS FILE WAS AUTOMATICALLY GENERATED, DO NOT MODIFY
########################################
Name: main_OnPush__CI_CD
RunMode: SUPERSEDED
SchemaVersion: "1.0"
Compute:
  Type: EC2
  Fleet: Linux.Arm64.2XLarge
Triggers:
  - Type: PUSH
    Branches:
      - main
Actions:
  Integration:
    Actions:
      Install:
        Identifier: aws/build@v1.0.0
        Timeout: 9
        Caching:
          FileCaching:
            a64_npm_global:
              Path: /tmp/npm-global
              RestoreKeys:
                - npminstall
            a64_pnpm_store:
              Path: /tmp/pnpm-store
              RestoreKeys:
                - pnpminstall
            a64_nx:
              Path: /tmp/nx-cache
              RestoreKeys:
                - nx
            a64_docker:
              Path: /tmp/docker-cache
              RestoreKeys:
                - docker
            a64_pulumi:
              Path: /tmp/pulumi
              RestoreKeys:
                - pulumi
        Inputs:
          Sources:
            - WorkflowSource
          Variables:
            - Name: NPM_REGISTRY_PROTOCOL
              Value: https
            - Name: NPM_REGISTRY_HOST
              Value: npm.pkg.github.com
            - Name: PAKETO_CLI_IMAGE
              Value: buildpacksio/pack:latest
            - Name: PAKETO_BUILDER_IMAGE
              Value: heroku/builder:24
            - Name: PAKETO_LAUNCHER_IMAGE
              Value: heroku/heroku:24
            - Name: PULUMI_VERSION
              Value: 3.144.1
            - Name: NODE_AUTH_TOKEN
              Value: ${Secrets.GITHUB_LEVICAPE_PAT}
        Configuration:
          Container:
            Registry: CODECATALYST
            Image: CodeCatalystLinux_Arm64:2024_03
          Steps:
            - Run: npm config set @levicape:registry=$NPM_REGISTRY_PROTOCOL://$NPM_REGISTRY_HOST --location project
            - Run: npm config set //$NPM_REGISTRY_HOST/:_authToken=$NODE_AUTH_TOKEN --location project
            - Run: npm config set prefix=/tmp/npm-global
            - Run: npm root -g
            - Run: npm install --g pnpm
            - Run: npm install --g n
            - Run: npm exec n 22
            - Run: npm exec pnpm config set store-dir /tmp/pnpm-store
            - Run: npm exec pnpm install
            - Run: mkdir -p /tmp/npm-global
            - Run: mkdir -p /tmp/pnpm-store
            - Run: mkdir -p /tmp/nx-cache
            - Run: mkdir -p /tmp/docker-cache
            - Run: mkdir -p /tmp/pulumi
            - Run: mkdir -p /tmp/docker-cache/images
            - Run: docker load --input /tmp/docker-cache/images/cli-pack.tar.gz || true
            - Run: docker pull $PAKETO_CLI_IMAGE
            - Run: docker save $PAKETO_CLI_IMAGE | gzip > /tmp/docker-cache/images/cli-pack.tar.gz
            - Run: du -sh /tmp/docker-cache/images/cli-pack.tar.gz
            - Run: docker load --input /tmp/docker-cache/images/builder.tar.gz || true
            - Run: docker pull $PAKETO_BUILDER_IMAGE
            - Run: docker save $PAKETO_BUILDER_IMAGE | gzip > /tmp/docker-cache/images/builder.tar.gz
            - Run: du -sh /tmp/docker-cache/images/builder.tar.gz
            - Run: docker load --input /tmp/docker-cache/images/launcher.tar.gz || true
            - Run: docker pull $PAKETO_LAUNCHER_IMAGE
            - Run: docker save $PAKETO_LAUNCHER_IMAGE | gzip > /tmp/docker-cache/images/launcher.tar.gz
            - Run: du -sh /tmp/docker-cache/images/launcher.tar.gz
            - Run: "[ -f /tmp/pulumi/bin/pulumi ] && /tmp/pulumi/bin/pulumi version | grep $PULUMI_VERSION || curl -fsSL https://get.pulumi.com | sh -s -- --version $PULUMI_VERSION --install-root /tmp/pulumi"
            - Run: du -sh /tmp/pulumi
            - Run: npm exec pnpm list
            - Run: du -sh node_modules /tmp/npm-global /tmp/pnpm-store
      Compile:
        Identifier: aws/build@v1.0.0
        DependsOn:
          - Install
        Timeout: 8
        Caching:
          FileCaching:
            a64_npm_global:
              Path: /tmp/npm-global
              RestoreKeys:
                - npminstall
            a64_pnpm_store:
              Path: /tmp/pnpm-store
              RestoreKeys:
                - pnpminstall
            a64_nx:
              Path: /tmp/nx-cache
              RestoreKeys:
                - nx
        Inputs:
          Sources:
            - WorkflowSource
        Outputs:
          AutoDiscoverReports:
            Enabled: true
            ReportNamePrefix: junit
        Configuration:
          Container:
            Registry: CODECATALYST
            Image: CodeCatalystLinux_Arm64:2024_03
          Steps:
            - Run: npm config set prefix=/tmp/npm-global
            - Run: npm exec n 22
            - Run: npm exec pnpm config set store-dir /tmp/pnpm-store
            - Run: npm exec pnpm install --prefer-offline
            - Run: npm exec pnpm build
            - Run: npm exec pnpm lint
            - Run: npm exec pnpm test
            - Run: du -sh $(pwd)/**/module $(pwd)/**/commonjs || true
      Image:
        Identifier: aws/build@v1.0.0
        DependsOn:
          - Install
        Timeout: 10
        Caching:
          FileCaching:
            a64_npm_global:
              Path: /tmp/npm-global
              RestoreKeys:
                - npminstall
            a64_pnpm_store:
              Path: /tmp/pnpm-store
              RestoreKeys:
                - pnpminstall
            a64_nx:
              Path: /tmp/nx-cache
              RestoreKeys:
                - nx
            a64_docker:
              Path: /tmp/docker-cache
              RestoreKeys:
                - docker
        Inputs:
          Sources:
            - WorkflowSource
          Variables:
            - Name: APPLICATION_IMAGE_NAME
              Value: spork
        Outputs:
          Artifacts:
            - Name: images
              Files:
                - _images/**/*
        Configuration:
          Container:
            Registry: CODECATALYST
            Image: CodeCatalystLinux_Arm64:2024_03
          Steps:
            - Run: docker load --input /tmp/docker-cache/images/cli-pack.tar.gz || true
            - Run: docker load --input /tmp/docker-cache/images/builder.tar.gz || true
            - Run: docker load --input /tmp/docker-cache/images/launcher.tar.gz || true
            - Run: mkdir -p _images
            - Run: npm config set prefix=/tmp/npm-global
            - Run: npm exec n 22
            - Run: npm exec pnpm config set store-dir /tmp/pnpm-store
            - Run: npm exec pnpm install --prefer-offline
            - Run: npm exec pnpm exec nx pack:build iac-images-application --verbose
            - Run: docker save $APPLICATION_IMAGE_NAME | gzip > _images/application.tar.gz
            - Run: du -sh _images/application.tar.gz
            - Run: ls -la _images
      Current:
        Identifier: aws/build@v1.0.0
        DependsOn:
          - Install
        Timeout: 10
        Environment:
          Name: current
        Caching:
          FileCaching:
            a64_npm_global:
              Path: /tmp/npm-global
              RestoreKeys:
                - npminstall
            a64_pnpm_store:
              Path: /tmp/pnpm-store
              RestoreKeys:
                - pnpminstall
            a64_nx:
              Path: /tmp/nx-cache
              RestoreKeys:
                - nx
            a64_pulumi:
              Path: /tmp/pulumi
              RestoreKeys:
                - pulumi
        Inputs:
          Sources:
            - WorkflowSource
          Variables:
            - Name: APPLICATION_IMAGE_NAME
              Value: spork
            - Name: CI_ENVIRONMENT
              Value: current
            - Name: AWS_REGION
              Value: us-west-2
            - Name: FRONTEND_HOSTNAME
              Value: levicape.cloud
            - Name: PULUMI_HOME
              Value: /tmp/pulumi
            - Name: PULUMI_CONFIG_PASSPHRASE
              Value: ${{ Secrets.PULUMI_CONFIG_PASSPHRASE }}
        Outputs:
          Artifacts:
            - Name: pulumi
              Files:
                - _pulumi/**/*
        Configuration:
          Container:
            Registry: CODECATALYST
            Image: CodeCatalystLinux_Arm64:2024_03
          Steps:
            - Run: npm config set prefix=/tmp/npm-global
            - Run: npm exec pnpm config set store-dir /tmp/pnpm-store
            - Run: npm exec n 22
            - Run: npm exec pnpm install --prefer-offline
            - Run: aws ssm get-parameter --name /fourtwo/_principal/pulumi/backend/StateBackendCommandReference
            - Run: npm exec pnpm exec fourtwo aws pulumi ci -- --region $AWS_REGION > .pulumi-ci
            - Run: cat .pulumi-ci
            - Run: cat .pulumi-ci | grep "export" >> .export-cd
            - Run: cat .export-cd
            - Run: source .export-cd
            - Run: mkdir _pulumi
            - Run: /tmp/pulumi/bin/pulumi stack init "$APPLICATION_IMAGE_NAME-codestar.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/codestar || true
            - Run: /tmp/pulumi/bin/pulumi stack select "$APPLICATION_IMAGE_NAME-codestar.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/codestar || true
            - Run: /tmp/pulumi/bin/pulumi config set aws:skipMetadataApiCheck false -C $(pwd)/iac/stacks/src/codestar
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.isProd false -C $(pwd)/iac/stacks/src/codestar
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.features aws -C $(pwd)/iac/stacks/src/codestar
            - Run: /tmp/pulumi/bin/pulumi config set --path 'frontend:stack.dns.hostnames[0]' "$CI_ENVIRONMENT.$APPLICATION_IMAGE_NAME.cloud.$FRONTEND_HOSTNAME" -C $(pwd)/iac/stacks/src/codestar
            - Run: /tmp/pulumi/bin/pulumi stack change-secrets-provider $AWS_PROVIDER_KEY -C $(pwd)/iac/stacks/src/codestar
            - Run: /tmp/pulumi/bin/pulumi preview -C $(pwd)/iac/stacks/src/codestar  --show-replacement-steps --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}"
            - Run: /tmp/pulumi/bin/pulumi up -C $(pwd)/iac/stacks/src/codestar --yes --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}"
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/codestar --json > $(pwd)/_pulumi/codestar.json
            - Run: cat _pulumi/codestar.json
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/codestar --shell > $(pwd)/_pulumi/codestar.sh
            - Run: cat _pulumi/codestar.sh
            - Run: /tmp/pulumi/bin/pulumi stack init "$APPLICATION_IMAGE_NAME-datalayer.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/datalayer || true
            - Run: /tmp/pulumi/bin/pulumi stack select "$APPLICATION_IMAGE_NAME-datalayer.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/datalayer || true
            - Run: /tmp/pulumi/bin/pulumi config set aws:skipMetadataApiCheck false -C $(pwd)/iac/stacks/src/datalayer
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.isProd false -C $(pwd)/iac/stacks/src/datalayer
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.features aws -C $(pwd)/iac/stacks/src/datalayer
            - Run: /tmp/pulumi/bin/pulumi config set --path 'frontend:stack.dns.hostnames[0]' "$CI_ENVIRONMENT.$APPLICATION_IMAGE_NAME.cloud.$FRONTEND_HOSTNAME" -C $(pwd)/iac/stacks/src/datalayer
            - Run: /tmp/pulumi/bin/pulumi stack change-secrets-provider $AWS_PROVIDER_KEY -C $(pwd)/iac/stacks/src/datalayer
            - Run: /tmp/pulumi/bin/pulumi preview -C $(pwd)/iac/stacks/src/datalayer  --show-replacement-steps --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}"
            - Run: /tmp/pulumi/bin/pulumi up -C $(pwd)/iac/stacks/src/datalayer --yes --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}"
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/datalayer --json > $(pwd)/_pulumi/datalayer.json
            - Run: cat _pulumi/datalayer.json
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/datalayer --shell > $(pwd)/_pulumi/datalayer.sh
            - Run: cat _pulumi/datalayer.sh
            - Run: /tmp/pulumi/bin/pulumi stack init "$APPLICATION_IMAGE_NAME-lambda.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/lambda || true
            - Run: /tmp/pulumi/bin/pulumi stack select "$APPLICATION_IMAGE_NAME-lambda.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/lambda || true
            - Run: /tmp/pulumi/bin/pulumi config set aws:skipMetadataApiCheck false -C $(pwd)/iac/stacks/src/lambda
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.isProd false -C $(pwd)/iac/stacks/src/lambda
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.features aws -C $(pwd)/iac/stacks/src/lambda
            - Run: /tmp/pulumi/bin/pulumi config set --path 'frontend:stack.dns.hostnames[0]' "$CI_ENVIRONMENT.$APPLICATION_IMAGE_NAME.cloud.$FRONTEND_HOSTNAME" -C $(pwd)/iac/stacks/src/lambda
            - Run: /tmp/pulumi/bin/pulumi stack change-secrets-provider $AWS_PROVIDER_KEY -C $(pwd)/iac/stacks/src/lambda
            - Run: /tmp/pulumi/bin/pulumi preview -C $(pwd)/iac/stacks/src/lambda  --show-replacement-steps --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}"
            - Run: /tmp/pulumi/bin/pulumi up -C $(pwd)/iac/stacks/src/lambda --yes --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}"
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/lambda --json > $(pwd)/_pulumi/lambda.json
            - Run: cat _pulumi/lambda.json
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/lambda --shell > $(pwd)/_pulumi/lambda.sh
            - Run: cat _pulumi/lambda.sh
            - Run: du -sh _pulumi
  Deployment:
    DependsOn:
      - Integration
    Actions:
      Lambda:
        Identifier: aws/build@v1.0.0
        Timeout: 10
        Environment:
          Name: current
        Caching:
          FileCaching:
            a64_npm_global:
              Path: /tmp/npm-global
              RestoreKeys:
                - npminstall
            a64_pnpm_store:
              Path: /tmp/pnpm-store
              RestoreKeys:
                - pnpminstall
            a64_nx:
              Path: /tmp/nx-cache
              RestoreKeys:
                - nx
        Inputs:
          Sources:
            - WorkflowSource
          Variables:
            - Name: APPLICATION_IMAGE_NAME
              Value: spork
            - Name: APPLICATION_IMAGE_TAG
              Value: levicape/spork
            - Name: AWS_REGION
              Value: us-west-2
            - Name: PULUMI_HOME
              Value: /tmp/pulumi
            - Name: PULUMI_CONFIG_PASSPHRASE
              Value: ${{ Secrets.PULUMI_CONFIG_PASSPHRASE }}
          Artifacts:
            - images
            - pulumi
        Configuration:
          Container:
            Registry: CODECATALYST
            Image: CodeCatalystLinux_Arm64:2024_03
          Steps:
            - Run: npm config set prefix=/tmp/npm-global
            - Run: npm exec pnpm config set store-dir /tmp/pnpm-store
            - Run: npm exec n 22
            - Run: npm exec pnpm install --prefer-offline
            - Run: ls -la $CATALYST_SOURCE_DIR_images/_images
            - Run: docker load --input $CATALYST_SOURCE_DIR_images/_images/application.tar.gz
            - Run: docker images
            - Run: ls -la $CATALYST_SOURCE_DIR_pulumi/_pulumi
            - Run: cat $CATALYST_SOURCE_DIR_pulumi/_pulumi/codestar.sh
            - Run: source $CATALYST_SOURCE_DIR_pulumi/_pulumi/codestar.sh
            - Run: cat $CATALYST_SOURCE_DIR_pulumi/_pulumi/datalayer.sh
            - Run: source $CATALYST_SOURCE_DIR_pulumi/_pulumi/datalayer.sh
            - Run: cat $CATALYST_SOURCE_DIR_pulumi/_pulumi/lambda.sh
            - Run: source $CATALYST_SOURCE_DIR_pulumi/_pulumi/lambda.sh
            - Run: env
            - Run: |-
                node -e '(function(){let imports=JSON.parse(process.env["_SPORK_LAMBDA_IMPORTS"]??"");let envs={"_SPORK_CODE_REPOSITORY_URL":imports.spork.codestar.ecr.repository.url};Object.entries(envs).forEach(([key,value])=>{process.stdout.write(`export ${key}="${value}"
                `)})})()' > .ci-env
            - Run: cat .ci-env
            - Run: source .ci-env
            - Run: export _AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
            - Run: aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $_SPORK_CODE_REPOSITORY_URL
            - Run: echo $APPLICATION_IMAGE_NAME
            - Run: echo ${WorkflowSource.CommitId}
            - Run: echo "Tagging $_SPORK_CODE_REPOSITORY_URL/$APPLICATION_IMAGE_TAG:${WorkflowSource.CommitId}"
            - Run: docker tag $APPLICATION_IMAGE_NAME:latest $_SPORK_CODE_REPOSITORY_URL/$APPLICATION_IMAGE_TAG:${WorkflowSource.CommitId}

########################################
########################################
#**:_$~- {"$$":"head","filename":"[push(b:main)]CI.yml","source":"iac/workflows/codecatalyst/push/CI.js"}
#**:_$~- {"$$":"script","generator":"codegen/codecatalyst/GenerateCodeCatalystWorkflow.mjs"}
#**:_$~- {"$$":"body","hashed":"a505f91c6adcda460e58452918a7407321423728cf3d723aad335f2e5b5e9cce"}
#**:_$~- {"$$":"footer","started":"2025-01-12T00:48:51.850Z","now":"2025-01-12T00:48:51.858Z","elapsed":"8ms"}
# END OF GENERATED FILE

