########################################
# THIS FILE WAS AUTOMATICALLY GENERATED, DO NOT MODIFY
########################################
name: "Dispatch: Delete"
on:
  workflow_dispatch:
    {}
env:
  NPM_REGISTRY_PROTOCOL: https
  NPM_REGISTRY_HOST: npm.pkg.github.com
  NODE_NO_WARNINGS: "1"
  NPM_CONFIG_UPDATE_NOTIFIER: "false"
  FRONTEND_HOSTNAME: at.levicape.cloud
  PULUMI_CONFIG_PASSPHRASE: ${{ secrets.PULUMI_CONFIG_PASSPHRASE }}
  APPLICATION_IMAGE_NAME: spork
  CI_ENVIRONMENT: elm_pst_3
  AWS_PAGER: ""
  AWS_REGION: us-west-2
  AWS_PROFILE: elm_pst_3
jobs:
  delete:
    name: Delete Pulumi Stacks
    runs-on: act-darwin-a64-atoko
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node 22.13.0
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          registry-url: ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          scope: "@levicape"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - id: cache-npm
        name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-npm-node-modules-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-build-npm-node-modules-
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV;
      - id: cache-pnpm-store
        name: Cache pnpm files
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-pnpm-store-
      - name: Set NPM Registry to Verdaccio:9200
        run: pnpm set registry http://localhost:9200/;
      - if: ${{ steps.cache-npm.outputs.cache-hit != 'true' }}
        name: List Dependencies
        continue-on-error: true
        run: pnpm list;
      - name: Install Dependencies
        run: pnpm install;
      - name: Reset Nx Cache
        run: pnpm exec nx reset || true;
      - name: Build image
        run: pnpm exec nx pack:build iac-images-application --verbose;
      - name: Verify AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
      - name: Setup Pulumi state backend
        run: |-
          echo "Retriving AWS credentials with pnpm exec fourtwo aws in $AWS_REGION";
          pnpm exec fourtwo;
          STRUCTURED_LOGGING=quiet pnpm exec fourtwo aws pulumi ci --region $AWS_REGION > .pulumi-ci;
      - name: Display Pulumi CI output
        run: cat .pulumi-ci;
      - name: Extract exports from Pulumi CI
        run: cat .pulumi-ci | grep "export" > .export-cd;
      - name: Load environment variables
        run: cat .export-cd;
      - name: Create Pulumi Helper Functions
        run: |-
          cat > .pulumi-helper.sh << 'EOF'
          configure_stack() {
            local step="$1"
            local stack_name="$2"
            local stack_cwd="$3"
            local project="$4"
            local output="$5"

            echo "${step}: Stack: ${stack_name}. CWD: ${stack_cwd}. Output: ${output}."
            echo "name: ${project}" >> "${stack_cwd}/Pulumi.yaml"
            cat "${stack_cwd}"/Pulumi.{yaml,"*".yaml} || true
          }

          setup_stack() {
            local stack_name="$1"
            local stack_cwd="$2"
            
            echo "Setting up stack: ${stack_name}. CWD: ${stack_cwd}."
            for cmd in init select; do
              pulumi stack ${cmd} ${stack_name} -C ${stack_cwd} || true
            done
          }

          configure_stack_settings() {
            local stack_cwd="$1"
            local configs="$2"
            
            echo "Configuring stack settings"
            
            while IFS= read -r line; do
              if [[ -n "$line" ]]; then
                key="${line%%=*}"
                value="${line#*=}"
                
                # Expand variables in value
                eval "value="$value""
                
                if [[ -n "$key" && -n "$value" ]]; then
                  echo "Setting $key to $value"
                  pulumi config set --path "$key" "$value" -C "$stack_cwd"
                fi
              fi
            done <<< "$configs"
          }

          refresh_and_preview() {
            local message="$1"
            local stack_cwd="$2"
            shift 2
            local default_args="$@"

            pulumi refresh --yes --skip-preview --clear-pending-creates --message "${message}-refresh" -C "${stack_cwd}" ${default_args}
            pulumi preview --show-replacement-steps --message "${message}-preview" -C "${stack_cwd}" ${default_args} || true
          }

          deploy_stack() {
            local message="$1"
            local stack_cwd="$2"
            shift 2
            local default_args="$@"

            pulumi up --yes --message "${message}-up" -C "${stack_cwd}" ${default_args}
          }

          remove_stack() {
          	local message="$1"
          	local stack_cwd="$2"
          	shift 2
          	local default_args="$@"
            
          	pulumi down --yes --message "${message}-down" -C "${stack_cwd}" ${default_args}
            }
            
          capture_outputs() {
            local stack_cwd="$1"
            local output="$2"

            pulumi stack output -C "${stack_cwd}" --json > "$(pwd)/${output}.json"
            cat "${output}.json"
            pulumi stack output -C "${stack_cwd}" --shell > "$(pwd)/${output}.sh"
            cat "${output}.sh"
            ls _pulumi
            chmod +x "$(pwd)/${output}.sh"
            echo "Output captured in ${output}.sh"
          }
          EOF
          chmod +x .pulumi-helper.sh
          source .pulumi-helper.sh;
      - name: Deploy spork stacks
        run: |-
          mkdir -p _pulumi;
          source .export-cd;
          echo '@levicape/fourtwo:';
          ls -la node_modules/@levicape/fourtwo || true;
          cat node_modules/@levicape/fourtwo/package.json || true;
          source .pulumi-helper.sh;
          configure_stack "Delete" "spork-application.elm_pst_3" "$(pwd)/iac/stacks/src/application" "spork-application" "_pulumi/application";
          setup_stack "spork-application.elm_pst_3" "$(pwd)/iac/stacks/src/application";
          configure_stack_settings "$(pwd)/iac/stacks/src/application" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws';
          remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/application" --non-interactive --suppress-progress --diff --json;
          configure_stack "Delete" "spork-codestar.elm_pst_3" "$(pwd)/iac/stacks/src/codestar" "spork-codestar" "_pulumi/codestar";
          setup_stack "spork-codestar.elm_pst_3" "$(pwd)/iac/stacks/src/codestar";
          configure_stack_settings "$(pwd)/iac/stacks/src/codestar" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws';
          remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/codestar" --non-interactive --suppress-progress --diff --json;
          configure_stack "Delete" "spork-datalayer.elm_pst_3" "$(pwd)/iac/stacks/src/datalayer" "spork-datalayer" "_pulumi/datalayer";
          setup_stack "spork-datalayer.elm_pst_3" "$(pwd)/iac/stacks/src/datalayer";
          configure_stack_settings "$(pwd)/iac/stacks/src/datalayer" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws';
          remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/datalayer" --non-interactive --suppress-progress --diff --json;
          configure_stack "Delete" "spork-http.elm_pst_3" "$(pwd)/iac/stacks/src/http" "spork-http" "_pulumi/http";
          setup_stack "spork-http.elm_pst_3" "$(pwd)/iac/stacks/src/http";
          configure_stack_settings "$(pwd)/iac/stacks/src/http" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws';
          remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/http" --non-interactive --suppress-progress --diff --json;
          configure_stack "Delete" "spork-magmap-http.elm_pst_3" "$(pwd)/iac/stacks/src/domains/magmap/http" "spork-magmap-http" "_pulumi/domains_magmap_http";
          setup_stack "spork-magmap-http.elm_pst_3" "$(pwd)/iac/stacks/src/domains/magmap/http";
          configure_stack_settings "$(pwd)/iac/stacks/src/domains/magmap/http" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws';
          remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/domains/magmap/http" --non-interactive --suppress-progress --diff --json;
          configure_stack "Delete" "spork-magmap-web.elm_pst_3" "$(pwd)/iac/stacks/src/domains/magmap/web" "spork-magmap-web" "_pulumi/domains_magmap_web";
          setup_stack "spork-magmap-web.elm_pst_3" "$(pwd)/iac/stacks/src/domains/magmap/web";
          configure_stack_settings "$(pwd)/iac/stacks/src/domains/magmap/web" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws';
          remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/domains/magmap/web" --non-interactive --suppress-progress --diff --json;
          configure_stack "Delete" "spork-magmap-monitor.elm_pst_3" "$(pwd)/iac/stacks/src/domains/magmap/monitor" "spork-magmap-monitor" "_pulumi/domains_magmap_monitor";
          setup_stack "spork-magmap-monitor.elm_pst_3" "$(pwd)/iac/stacks/src/domains/magmap/monitor";
          configure_stack_settings "$(pwd)/iac/stacks/src/domains/magmap/monitor" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws';
          remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/domains/magmap/monitor" --non-interactive --suppress-progress --diff --json;
          configure_stack "Delete" "spork-magmap-wwwroot.elm_pst_3" "$(pwd)/iac/stacks/src/domains/magmap/wwwroot" "spork-magmap-wwwroot" "_pulumi/domains_magmap_wwwroot";
          setup_stack "spork-magmap-wwwroot.elm_pst_3" "$(pwd)/iac/stacks/src/domains/magmap/wwwroot";
          configure_stack_settings "$(pwd)/iac/stacks/src/domains/magmap/wwwroot" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws';
          remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/domains/magmap/wwwroot" --non-interactive --suppress-progress --diff --json;
      - name: Cleanup
        run: |-
          rm -f .pulumi-ci .export-cd .pulumi-helper.sh .ci-env;
          rm -rf _pulumi;
          pnpm store prune || true;
          corepack cache clean || true;

########################################
########################################
#**:_$~- {"$$":"head","filename":"[wd][dispatch] - CD - [Dispatch: Delete].yml","source":"iac/workflows/github/workflow_dispatch/CD.js"}
#**:_$~- {"$$":"script","generator":"codegen/github/GenerateGithubWorkflow.mjs"}
#**:_$~- {"$$":"body","hashed":"05cfbfcc709dee3e06d566dad7749c8181fd3f6ea54a219562742fed56ce7c05"}
#**:_$~- {"$$":"footer","started":"2025-03-09T02:31:11.258Z","now":"2025-03-09T02:31:11.261Z","elapsed":"3ms"}
# END OF GENERATED FILE

