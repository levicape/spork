########################################
# THIS FILE WAS AUTOMATICALLY GENERATED, DO NOT MODIFY
########################################
name: "elm_pst_3 Dispatch: Delete"
on:
  workflow_dispatch:
    {}
env:
  NPM_REGISTRY_PROTOCOL_LEVICAPE: https
  NPM_REGISTRY_HOST_LEVICAPE: npm.pkg.github.com
  NPM_TOKEN_LEVICAPE: ${{ secrets.LEVICAPE_NPM_TOKEN }}
  NPM_DEFAULT: ${{ vars.NPM_MIRROR }}
  LEVICAPE_REGISTRY_HOST: npm.pkg.github.com/
  LEVICAPE_REGISTRY: https://npm.pkg.github.com
  LEVICAPE_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  NODE_NO_WARNINGS: "1"
  NPM_CONFIG_UPDATE_NOTIFIER: "false"
  FRONTEND_HOSTNAME: at.levicape.cloud
  PULUMI_CONFIG_PASSPHRASE: ${{ secrets.PULUMI_CONFIG_PASSPHRASE }}
  APPLICATION_IMAGE_NAME: spork
  CI_ENVIRONMENT: elm_pst_3
  AWS_PAGER: ""
  AWS_REGION: us-west-2
  AWS_PROFILE: elm_pst_3
  PULUMI_STACK_FILTER: ${{ vars.STACK_FILTER }}
  DOCKER_NO_IMAGE: ${{ vars.NO_IMAGE }}
jobs:
  delete:
    name: Delete Pulumi Stacks
    runs-on: act-darwin-a64-atoko
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node 22.13.0
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          registry-url: ${{ env.NPM_REGISTRY_PROTOCOL_LEVICAPE }}://${{ env.NPM_REGISTRY_HOST_LEVICAPE }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          scope: "@levicape"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - id: cache-npm
        name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-npm-node-modules-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-build-npm-node-modules-
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV;
      - id: cache-pnpm-store
        name: Cache pnpm files
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-pnpm-store-
      - name: Set NPM Registry to Verdaccio:31313 or NPM_MIRROR
        run: pnpm set registry ${NPM_DEFAULT:-http://localhost:31313};
      - if: ${{ steps.cache-npm.outputs.cache-hit != 'true' }}
        name: List Dependencies
        continue-on-error: true
        run: pnpm list;
      - name: Install Dependencies
        run: pnpm install;
      - name: Build image
        run: |-
          if [ -z "$DOCKER_NO_IMAGE" ]; then 
          													pnpm exec nx pack:build iac-images-application --verbose; 
          												fi;
      - name: Verify AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
      - name: Setup Pulumi state backend
        run: |-
          echo "Retriving AWS credentials with pnpm exec fourtwo aws in $AWS_REGION";
          pnpm exec fourtwo;
          STRUCTURED_LOGGING=quiet pnpm exec fourtwo aws pulumi ci --region $AWS_REGION > .pulumi-ci;
      - name: Display Pulumi CI output
        run: cat .pulumi-ci;
      - name: Extract exports from Pulumi CI
        run: cat .pulumi-ci | grep "export" > .export-cd;
      - name: Load environment variables
        run: cat .export-cd;
      - name: Create Pulumi Helper Functions
        run: |-
          cat > .pulumi-helper.sh << 'EOF'
          configure_stack() {
            local step="$1"
            local stack_name="$2"
            local stack_cwd="$3"
            local project="$4"
            local output="$5"

            echo "${step}: Stack: ${stack_name}. CWD: ${stack_cwd}. Output: ${output}."
            echo "name: ${project}" >> "${stack_cwd}/Pulumi.yaml"
            cat "${stack_cwd}"/Pulumi.{yaml,"*".yaml} || true
          }

          setup_stack() {
            local stack_name="$1"
            local stack_cwd="$2"
            
            echo "Setting up stack: ${stack_name}. CWD: ${stack_cwd}."
            for cmd in init select; do
              pulumi stack ${cmd} ${stack_name} -C ${stack_cwd} || true
            done
          }

          configure_stack_settings() {
            local stack_cwd="$1"
            local configs="$2"
            
            echo "Configuring stack settings"
            
            while IFS= read -r line; do
              if [[ -n "$line" ]]; then
                key="${line%%=*}"
                value="${line#*=}"
                
                # Expand variables in value
                eval "value="$value""
                
                if [[ -n "$key" && -n "$value" ]]; then
                  echo "Setting $key to $value"
                  pulumi config set --path "$key" "$value" -C "$stack_cwd"
                fi
              fi
            done <<< "$configs"
          }

          refresh_and_preview() {
            local message="$1"
            local stack_cwd="$2"
            shift 2
            local default_args="$@"

            pulumi refresh --yes --skip-preview --clear-pending-creates --message "${message}-refresh" -C "${stack_cwd}" ${default_args}
            pulumi preview --show-replacement-steps --message "${message}-preview" -C "${stack_cwd}" ${default_args} || true
          }

          deploy_stack() {
            local message="$1"
            local stack_cwd="$2"
            shift 2
            local default_args="$@"

            pulumi up --yes --message "${message}-up" -C "${stack_cwd}" ${default_args}
          }

          remove_stack() {
          	local message="$1"
          	local stack_cwd="$2"
          	shift 2
          	local default_args="$@"
            
          	pulumi down --yes --message "${message}-down" -C "${stack_cwd}" ${default_args}
            }
            
          capture_outputs() {
            local stack_cwd="$1"
            local output="$2"

            pulumi stack output -C "${stack_cwd}" --json > "$(pwd)/${output}.json"
            cat "${output}.json"
            pulumi stack output -C "${stack_cwd}" --shell > "$(pwd)/${output}.sh"
            cat "${output}.sh"
            ls _pulumi
            chmod +x "$(pwd)/${output}.sh"
            echo "Output captured in ${output}.sh"
          }
          EOF
          chmod +x .pulumi-helper.sh
          source .pulumi-helper.sh;
      - name: Deploy spork stacks
        run: |-
          mkdir -p _pulumi;
          source .export-cd;
          echo '@levicape/fourtwo:';
          ls -la node_modules/@levicape/fourtwo || true;
          cat node_modules/@levicape/fourtwo/package.json || true;
          source .pulumi-helper.sh;
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-wwwroot" ]; then
          															echo "Stack spork-magmap-wwwroot matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-wwwroot,"; then
          															echo "Stack spork-magmap-wwwroot found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-wwwroot not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-magmap-wwwroot.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/wwwroot" "spork-magmap-wwwroot" "_pulumi/levicape_magmap_wwwroot";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-wwwroot" ]; then
          															echo "Stack spork-magmap-wwwroot matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-wwwroot,"; then
          															echo "Stack spork-magmap-wwwroot found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-wwwroot not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-magmap-wwwroot.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/wwwroot";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-wwwroot" ]; then
          															echo "Stack spork-magmap-wwwroot matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-wwwroot,"; then
          															echo "Stack spork-magmap-wwwroot found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-wwwroot not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/levicape/magmap/wwwroot" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-wwwroot" ]; then
          															echo "Stack spork-magmap-wwwroot matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-wwwroot,"; then
          															echo "Stack spork-magmap-wwwroot found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-wwwroot not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/levicape/magmap/wwwroot" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-magmap-wwwroot.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-monitor" ]; then
          															echo "Stack spork-magmap-monitor matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-monitor,"; then
          															echo "Stack spork-magmap-monitor found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-monitor not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-magmap-monitor.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/monitor" "spork-magmap-monitor" "_pulumi/levicape_magmap_monitor";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-monitor" ]; then
          															echo "Stack spork-magmap-monitor matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-monitor,"; then
          															echo "Stack spork-magmap-monitor found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-monitor not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-magmap-monitor.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/monitor";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-monitor" ]; then
          															echo "Stack spork-magmap-monitor matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-monitor,"; then
          															echo "Stack spork-magmap-monitor found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-monitor not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/levicape/magmap/monitor" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-monitor" ]; then
          															echo "Stack spork-magmap-monitor matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-monitor,"; then
          															echo "Stack spork-magmap-monitor found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-monitor not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/levicape/magmap/monitor" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-magmap-monitor.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-web" ]; then
          															echo "Stack spork-magmap-web matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-web,"; then
          															echo "Stack spork-magmap-web found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-web not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-magmap-web.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/web" "spork-magmap-web" "_pulumi/levicape_magmap_web";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-web" ]; then
          															echo "Stack spork-magmap-web matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-web,"; then
          															echo "Stack spork-magmap-web found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-web not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-magmap-web.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/web";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-web" ]; then
          															echo "Stack spork-magmap-web matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-web,"; then
          															echo "Stack spork-magmap-web found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-web not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/levicape/magmap/web" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-web" ]; then
          															echo "Stack spork-magmap-web matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-web,"; then
          															echo "Stack spork-magmap-web found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-web not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/levicape/magmap/web" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-magmap-web.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-http" ]; then
          															echo "Stack spork-magmap-http matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-http,"; then
          															echo "Stack spork-magmap-http found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-http not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-magmap-http.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/http" "spork-magmap-http" "_pulumi/levicape_magmap_http";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-http" ]; then
          															echo "Stack spork-magmap-http matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-http,"; then
          															echo "Stack spork-magmap-http found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-http not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-magmap-http.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/http";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-http" ]; then
          															echo "Stack spork-magmap-http matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-http,"; then
          															echo "Stack spork-magmap-http found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-http not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/levicape/magmap/http" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-http" ]; then
          															echo "Stack spork-magmap-http matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-http,"; then
          															echo "Stack spork-magmap-http found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-http not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/levicape/magmap/http" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-magmap-http.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-http" ]; then
          															echo "Stack spork-http matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-http,"; then
          															echo "Stack spork-http found in comma-separated list"
          															true
          														else
          															echo "Stack spork-http not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-http.elm_pst_3" "$(pwd)/iac/stacks/src/http" "spork-http" "_pulumi/http";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-http" ]; then
          															echo "Stack spork-http matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-http,"; then
          															echo "Stack spork-http found in comma-separated list"
          															true
          														else
          															echo "Stack spork-http not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-http.elm_pst_3" "$(pwd)/iac/stacks/src/http";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-http" ]; then
          															echo "Stack spork-http matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-http,"; then
          															echo "Stack spork-http found in comma-separated list"
          															true
          														else
          															echo "Stack spork-http not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/http" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-http" ]; then
          															echo "Stack spork-http matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-http,"; then
          															echo "Stack spork-http found in comma-separated list"
          															true
          														else
          															echo "Stack spork-http not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/http" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-http.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-client" ]; then
          															echo "Stack spork-magmap-client matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-client,"; then
          															echo "Stack spork-magmap-client found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-client not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-magmap-client.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/client" "spork-magmap-client" "_pulumi/levicape_magmap_client";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-client" ]; then
          															echo "Stack spork-magmap-client matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-client,"; then
          															echo "Stack spork-magmap-client found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-client not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-magmap-client.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/client";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-client" ]; then
          															echo "Stack spork-magmap-client matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-client,"; then
          															echo "Stack spork-magmap-client found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-client not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/levicape/magmap/client" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-client" ]; then
          															echo "Stack spork-magmap-client matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-client,"; then
          															echo "Stack spork-magmap-client found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-client not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/levicape/magmap/client" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-magmap-client.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-channels" ]; then
          															echo "Stack spork-magmap-channels matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-channels,"; then
          															echo "Stack spork-magmap-channels found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-channels not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-magmap-channels.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/channels" "spork-magmap-channels" "_pulumi/levicape_magmap_channels";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-channels" ]; then
          															echo "Stack spork-magmap-channels matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-channels,"; then
          															echo "Stack spork-magmap-channels found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-channels not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-magmap-channels.elm_pst_3" "$(pwd)/iac/stacks/src/levicape/magmap/channels";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-channels" ]; then
          															echo "Stack spork-magmap-channels matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-channels,"; then
          															echo "Stack spork-magmap-channels found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-channels not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/levicape/magmap/channels" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-magmap-channels" ]; then
          															echo "Stack spork-magmap-channels matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-magmap-channels,"; then
          															echo "Stack spork-magmap-channels found in comma-separated list"
          															true
          														else
          															echo "Stack spork-magmap-channels not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/levicape/magmap/channels" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-magmap-channels.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-dns-root" ]; then
          															echo "Stack spork-dns-root matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-dns-root,"; then
          															echo "Stack spork-dns-root found in comma-separated list"
          															true
          														else
          															echo "Stack spork-dns-root not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-dns-root.elm_pst_3" "$(pwd)/iac/stacks/src/dns/root" "spork-dns-root" "_pulumi/dns_root";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-dns-root" ]; then
          															echo "Stack spork-dns-root matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-dns-root,"; then
          															echo "Stack spork-dns-root found in comma-separated list"
          															true
          														else
          															echo "Stack spork-dns-root not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-dns-root.elm_pst_3" "$(pwd)/iac/stacks/src/dns/root";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-dns-root" ]; then
          															echo "Stack spork-dns-root matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-dns-root,"; then
          															echo "Stack spork-dns-root found in comma-separated list"
          															true
          														else
          															echo "Stack spork-dns-root not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/dns/root" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-dns-root" ]; then
          															echo "Stack spork-dns-root matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-dns-root,"; then
          															echo "Stack spork-dns-root found in comma-separated list"
          															true
          														else
          															echo "Stack spork-dns-root not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/dns/root" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-dns-root.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-datalayer" ]; then
          															echo "Stack spork-datalayer matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-datalayer,"; then
          															echo "Stack spork-datalayer found in comma-separated list"
          															true
          														else
          															echo "Stack spork-datalayer not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-datalayer.elm_pst_3" "$(pwd)/iac/stacks/src/datalayer" "spork-datalayer" "_pulumi/datalayer";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-datalayer" ]; then
          															echo "Stack spork-datalayer matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-datalayer,"; then
          															echo "Stack spork-datalayer found in comma-separated list"
          															true
          														else
          															echo "Stack spork-datalayer not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-datalayer.elm_pst_3" "$(pwd)/iac/stacks/src/datalayer";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-datalayer" ]; then
          															echo "Stack spork-datalayer matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-datalayer,"; then
          															echo "Stack spork-datalayer found in comma-separated list"
          															true
          														else
          															echo "Stack spork-datalayer not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/datalayer" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-datalayer" ]; then
          															echo "Stack spork-datalayer matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-datalayer,"; then
          															echo "Stack spork-datalayer found in comma-separated list"
          															true
          														else
          															echo "Stack spork-datalayer not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/datalayer" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-datalayer.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-identity-users" ]; then
          															echo "Stack spork-identity-users matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-identity-users,"; then
          															echo "Stack spork-identity-users found in comma-separated list"
          															true
          														else
          															echo "Stack spork-identity-users not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-identity-users.elm_pst_3" "$(pwd)/iac/stacks/src/identity/users" "spork-identity-users" "_pulumi/identity_users";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-identity-users" ]; then
          															echo "Stack spork-identity-users matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-identity-users,"; then
          															echo "Stack spork-identity-users found in comma-separated list"
          															true
          														else
          															echo "Stack spork-identity-users not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-identity-users.elm_pst_3" "$(pwd)/iac/stacks/src/identity/users";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-identity-users" ]; then
          															echo "Stack spork-identity-users matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-identity-users,"; then
          															echo "Stack spork-identity-users found in comma-separated list"
          															true
          														else
          															echo "Stack spork-identity-users not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/identity/users" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-identity-users" ]; then
          															echo "Stack spork-identity-users matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-identity-users,"; then
          															echo "Stack spork-identity-users found in comma-separated list"
          															true
          														else
          															echo "Stack spork-identity-users not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/identity/users" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-identity-users.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-identity-oidc" ]; then
          															echo "Stack spork-identity-oidc matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-identity-oidc,"; then
          															echo "Stack spork-identity-oidc found in comma-separated list"
          															true
          														else
          															echo "Stack spork-identity-oidc not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-identity-oidc.elm_pst_3" "$(pwd)/iac/stacks/src/identity/oidc" "spork-identity-oidc" "_pulumi/identity_oidc";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-identity-oidc" ]; then
          															echo "Stack spork-identity-oidc matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-identity-oidc,"; then
          															echo "Stack spork-identity-oidc found in comma-separated list"
          															true
          														else
          															echo "Stack spork-identity-oidc not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-identity-oidc.elm_pst_3" "$(pwd)/iac/stacks/src/identity/oidc";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-identity-oidc" ]; then
          															echo "Stack spork-identity-oidc matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-identity-oidc,"; then
          															echo "Stack spork-identity-oidc found in comma-separated list"
          															true
          														else
          															echo "Stack spork-identity-oidc not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/identity/oidc" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-identity-oidc" ]; then
          															echo "Stack spork-identity-oidc matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-identity-oidc,"; then
          															echo "Stack spork-identity-oidc found in comma-separated list"
          															true
          														else
          															echo "Stack spork-identity-oidc not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/identity/oidc" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-identity-oidc.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-codestar" ]; then
          															echo "Stack spork-codestar matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-codestar,"; then
          															echo "Stack spork-codestar found in comma-separated list"
          															true
          														else
          															echo "Stack spork-codestar not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-codestar.elm_pst_3" "$(pwd)/iac/stacks/src/codestar" "spork-codestar" "_pulumi/codestar";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-codestar" ]; then
          															echo "Stack spork-codestar matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-codestar,"; then
          															echo "Stack spork-codestar found in comma-separated list"
          															true
          														else
          															echo "Stack spork-codestar not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-codestar.elm_pst_3" "$(pwd)/iac/stacks/src/codestar";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-codestar" ]; then
          															echo "Stack spork-codestar matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-codestar,"; then
          															echo "Stack spork-codestar found in comma-separated list"
          															true
          														else
          															echo "Stack spork-codestar not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/codestar" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-codestar" ]; then
          															echo "Stack spork-codestar matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-codestar,"; then
          															echo "Stack spork-codestar found in comma-separated list"
          															true
          														else
          															echo "Stack spork-codestar not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/codestar" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-codestar.elm_pst_3 processed";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-application" ]; then
          															echo "Stack spork-application matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-application,"; then
          															echo "Stack spork-application found in comma-separated list"
          															true
          														else
          															echo "Stack spork-application not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack "Delete" "spork-application.elm_pst_3" "$(pwd)/iac/stacks/src/application" "spork-application" "_pulumi/application";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-application" ]; then
          															echo "Stack spork-application matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-application,"; then
          															echo "Stack spork-application found in comma-separated list"
          															true
          														else
          															echo "Stack spork-application not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && setup_stack "spork-application.elm_pst_3" "$(pwd)/iac/stacks/src/application";
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-application" ]; then
          															echo "Stack spork-application matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-application,"; then
          															echo "Stack spork-application found in comma-separated list"
          															true
          														else
          															echo "Stack spork-application not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && configure_stack_settings "$(pwd)/iac/stacks/src/application" 'aws:skipMetadataApiCheck=false
          context:stack.environment.isProd=false
          context:stack.environment.features=aws
          frontend:stack.dns.hostnames[0]=elm_pst_3.spork.$FRONTEND_HOSTNAME';
          if [ -n "$PULUMI_STACK_FILTER" ]; then
          														if [ "$PULUMI_STACK_FILTER" = "*" ]; then
          															echo "Running all stacks due to wildcard filter"
          															true
          														elif [ "$PULUMI_STACK_FILTER" = "spork-application" ]; then
          															echo "Stack spork-application matched in filter"
          															true
          														elif echo ",$PULUMI_STACK_FILTER," | grep -q ",spork-application,"; then
          															echo "Stack spork-application found in comma-separated list"
          															true
          														else
          															echo "Stack spork-application not in filter '$PULUMI_STACK_FILTER', skipping"
          															false
          														fi
          													else
          														echo "No stack filter specified, processing all stacks"
          														true
          													fi && remove_stack "${{ github.ref_name }}-${{ github.sha }}" "$(pwd)/iac/stacks/src/application" --non-interactive --suppress-progress --diff --json;
          echo "Stack spork-application.elm_pst_3 processed";
      - name: Cleanup
        run: |-
          rm -f .pulumi-ci .export-cd .pulumi-helper.sh .ci-env;
          rm -rf _pulumi;

########################################
########################################
#**:_$~- {"$$":"head","filename":"[wd][dispatch] - CD - [elm_pst_3 Dispatch: Delete].yml","source":"iac/workflows/github/workflow_dispatch/CD.js"}
#**:_$~- {"$$":"script","generator":"codegen/github/GenerateGithubWorkflow.mjs"}
#**:_$~- {"$$":"body","hashed":"750427f88f3c5af2a962cb8abbbdb7005863d1625c8a92f8a4516179915e9a8f"}
#**:_$~- {"$$":"footer","started":"2025-03-25T01:36:39.413Z","now":"2025-03-25T01:36:39.416Z","elapsed":"3ms"}
# END OF GENERATED FILE

