{
	"name": "iac-images-application",
	"$schema": "../../../node_modules/nx/schemas/nx-schema.json",
	"targets": {
		"pack:build": {
			"executor": "nx:run-commands",
			"options": {
				"env": {
					"DEFAULT_IMAGE_NAME": "spork",
					"PAKETO_BUILDER": "heroku/builder:24"
				},
				"commands": [
					"npx nx run iac-images:cli:pack build $DEFAULT_IMAGE_NAME --builder $PAKETO_BUILDER --default-process cli  --cache 'type=build;format=bind;source=/tmp/docker-cache;type=launch;format=volume;'"
				]
			}
		}
	}
}
